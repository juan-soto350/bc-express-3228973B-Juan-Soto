services:
  mongo:
    image: mongo:7
    container_name: arcade-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: arcade
      MONGO_INITDB_ROOT_PASSWORD: arcade123
      MONGO_INITDB_DATABASE: arcade_dev
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
